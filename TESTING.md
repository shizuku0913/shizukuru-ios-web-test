# Shizukuru Mixing Lab 自動テスト

v10.8.14 から導入した、アプリ本体を変更しない最小構成の回帰テストです。

## 実行方法

Node.js 18 以降でプロジェクト直下から:

```bash
npm test
```

追加パッケージは不要です。Node.js 標準の `node:test` を使用します。

## 現在守っているもの

- v10.8.14 の「同じレシピだけでは同じ色とみなさない」ゲート
- 個人研究色の再現は現在の完成色が許容距離内であること
- 公式色の再発見は AdaptiveOfficialDiscovery を通ること
- commit 経路と startProvisionalDiscovery 経路の両方に色ゲートが存在すること
- 公式色図鑑が 350 色・名称重複なしであること
- ΔE / hue / chroma / adaptive-radius の判定ガードが消えていないこと

## 方針

この段階では `index.html` の実装を `colorEngine.js` へ移していません。テスト側が実際の `index.html` から v10.8.14 の関数を読み出して検証します。これにより、テスト導入そのものがAndroid/WebViewの実行挙動を変えないようにしています。

今後、色判定を外部JSへ整理する場合は、このテストが通ることを確認しながら段階的に移行します。過去に発生した色判定バグは、修正時に1件ずつ回帰テストとして追加してください。
